
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id        String   @id @default(uuid())
  email     String   @unique
  name      String?
  password  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  categories           Category[]
  budgets              Budget[]
  tasks                Task[]
  savingsGoals         SavingsGoal[]
  accounts             Account[]
  transactions         Transaction[]
  investments          Investment[]
  debts                Debt[]
  subscriptions        Subscription[]
  goals                Goal[]
  reminders            Reminder[]
  journalEntries       JournalEntry[]        // Future
  habits               Habit[]               // Future
  learningProgresses   LearningProgress[]    // Future
  visionBoards         VisionBoard[]         // Future
  mindMaps             MindMap[]             // Future
  exerciseLogs         ExerciseLog[]         // Future
  nutritionLogs        NutritionLog[]        // Future
  sleepLogs            SleepLog[]            // Future
  waterIntakeLogs      WaterIntakeLog[]      // Future
  meditationLogs       MeditationLog[]       // Future
  symptomLogs          SymptomLog[]          // Future
  events               Event[]               // Future
  calendars            Calendar[]            // Future
  contacts             Contact[]             // Future
  chores               Chore[]               // Future
  shoppingLists        ShoppingList[]        // Future
  inventoryItems       InventoryItem[]       // Future
  travelPlans          TravelPlan[]          // Future
  petCareLogs          PetCareLog[]          // Future
  pets                 Pet[]                 // Future
  smartHomeIntegrations SmartHomeIntegration[] // Future
  careerTrackers       CareerTracker[]       // Future
  networkReminders     NetworkReminder[]     // Future
  creativeNotes        CreativeNote[]        // Future
}

model Category {
  id        String   @id @default(uuid())
  userId    String
  name      String
  type      String?  // income, expense, budget
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user         User          @relation(fields: [userId], references: [id])
  budgets      Budget[]
  transactions Transaction[]
  subscriptions Subscription[]
}

model Budget {
  id        String   @id @default(uuid())
  userId    String
  categoryId String
  amount    Float
  period    String   // monthly, yearly
  startDate DateTime
  endDate   DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user      User     @relation(fields: [userId], references: [id])
  category  Category @relation(fields: [categoryId], references: [id])
  reminders Reminder[]
}

model Task {
  id           String   @id @default(uuid())
  userId       String
  title        String
  description  String?
  priority     Int      // 1-5
  dueDate      DateTime?
  status       String   // todo, in-progress, done
  reminderTime DateTime?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  user      User     @relation(fields: [userId], references: [id])
  goal      Goal?    @relation(fields: [goalId], references: [id])
  goalId    String?
  reminders Reminder[]
}

model SavingsGoal {
  id            String   @id @default(uuid())
  userId        String
  name          String
  targetAmount  Float
  currentAmount Float    @default(0)
  deadline      DateTime?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model Account {
  id        String   @id @default(uuid())
  userId    String
  name      String
  type      String   // bank, savings, checking
  balance   Float    @default(0)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user         User         @relation(fields: [userId], references: [id])
  transactions Transaction[]
  deposits     Deposit[]
}

model Transaction {
  id          String   @id @default(uuid())
  userId      String
  accountId   String?
  categoryId  String?
  amount      Float
  date        DateTime
  type        String   // income, expense, transfer
  recurring   Boolean  @default(false)
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user      User      @relation(fields: [userId], references: [id])
  account   Account?  @relation(fields: [accountId], references: [id])
  category  Category? @relation(fields: [categoryId], references: [id])
}

model Deposit {
  id          String   @id @default(uuid())
  accountId   String
  amount      Float
  date        DateTime
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  account Account @relation(fields: [accountId], references: [id])
}

model Investment {
  id            String   @id @default(uuid())
  userId        String
  name          String
  type          String   // stock, fund
  purchaseAmount Float
  currentValue  Float
  purchaseDate  DateTime
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model Debt {
  id           String   @id @default(uuid())
  userId       String
  creditor     String
  amount       Float
  interestRate Float?
  dueDate      DateTime?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  user     User     @relation(fields: [userId], references: [id])
  payments Payment[]
}

model Payment {
  id        String   @id @default(uuid())
  debtId    String
  amount    Float
  date      DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  debt Debt @relation(fields: [debtId], references: [id])
}

model Subscription {
  id          String   @id @default(uuid())
  userId      String
  name        String
  amount      Float
  frequency   String   // monthly, yearly
  nextDueDate DateTime
  categoryId  String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user      User      @relation(fields: [userId], references: [id])
  category  Category? @relation(fields: [categoryId], references: [id])
  reminders Reminder[]
}

model Goal {
  id          String   @id @default(uuid())
  userId      String
  name        String
  description String?
  targetDate  DateTime?
  progress    Float    @default(0)
  type        String?  // financial, personal
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user  User  @relation(fields: [userId], references: [id])
  tasks Task[]
}

model Reminder {
  id          String   @id @default(uuid())
  userId      String
  type        String   // task, budget, subscription
  message     String
  triggerTime DateTime
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user         User         @relation(fields: [userId], references: [id])
  task         Task?        @relation(fields: [taskId], references: [id])
  taskId       String?
  budget       Budget?      @relation(fields: [budgetId], references: [id])
  budgetId     String?
  subscription Subscription? @relation(fields: [subscriptionId], references: [id])
  subscriptionId String?
}

// Future Models

model JournalEntry {
  id        String   @id @default(uuid())
  userId    String
  content   String
  date      DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model Habit {
  id        String   @id @default(uuid())
  userId    String
  name      String
  frequency String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user      User      @relation(fields: [userId], references: [id])
  habitLogs HabitLog[]
}

model HabitLog {
  id        String   @id @default(uuid())
  habitId   String
  date      DateTime
  completed Boolean
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  habit Habit @relation(fields: [habitId], references: [id])
}

model LearningProgress {
  id        String   @id @default(uuid())
  userId    String
  topic     String
  progress  Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user             User             @relation(fields: [userId], references: [id])
  learningSessions LearningSession[]
}

model LearningSession {
  id                  String   @id @default(uuid())
  learningProgressId  String
  startTime           DateTime
  endTime             DateTime
  notes               String?
  createdAt           DateTime @default(now())
  updatedAt           DateTime @updatedAt

  learningProgress LearningProgress @relation(fields: [learningProgressId], references: [id])
}

model VisionBoard {
  id        String   @id @default(uuid())
  userId    String
  title     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user        User        @relation(fields: [userId], references: [id])
  visionItems VisionItem[]
}

model VisionItem {
  id            String   @id @default(uuid())
  visionBoardId String
  description   String
  imageUrl      String?
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt

  visionBoard VisionBoard @relation(fields: [visionBoardId], references: [id])
}

model MindMap {
  id        String   @id @default(uuid())
  userId    String
  title     String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user          User          @relation(fields: [userId], references: [id])
  mindMapNodes  MindMapNode[]
}

model MindMapNode {
  id          String   @id @default(uuid())
  mindMapId   String
  parentNodeId String?
  content     String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  mindMap     MindMap     @relation(fields: [mindMapId], references: [id])
  parent      MindMapNode? @relation("MindMapNodeToMindMapNode", fields: [parentNodeId], references: [id])
  children    MindMapNode[] @relation("MindMapNodeToMindMapNode")
}

model ExerciseLog {
  id              String   @id @default(uuid())
  userId          String
  date            DateTime
  type            String
  durationMinutes Int
  caloriesBurned  Float?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model NutritionLog {
  id        String   @id @default(uuid())
  userId    String
  date      DateTime
  calories  Float
  protein   Float
  carbs     Float
  fats      Float
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user      User      @relation(fields: [userId], references: [id])
  foodItems FoodItem[]
}

model FoodItem {
  id             String   @id @default(uuid())
  nutritionLogId String
  name           String
  quantity       Float
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  nutritionLog NutritionLog @relation(fields: [nutritionLogId], references: [id])
}

model SleepLog {
  id        String   @id @default(uuid())
  userId    String
  date      DateTime
  startTime DateTime
  endTime   DateTime
  quality   Int?     // 1-10
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model WaterIntakeLog {
  id           String   @id @default(uuid())
  userId       String
  date         DateTime
  amountLiters Float
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model MeditationLog {
  id              String   @id @default(uuid())
  userId          String
  date            DateTime
  durationMinutes Int
  type            String?
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model SymptomLog {
  id        String   @id @default(uuid())
  userId    String
  date      DateTime
  symptom   String
  severity  Int      // 1-10
  notes     String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model Event {
  id        String   @id @default(uuid())
  userId    String
  title     String
  startTime DateTime
  endTime   DateTime?
  location  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model Calendar {
  id        String   @id @default(uuid())
  userId    String      // Owner
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model Contact {
  id          String   @id @default(uuid())
  userId      String
  name        String
  email       String?
  phone       String?
  birthday    DateTime?
  preferences String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user             User             @relation(fields: [userId], references: [id])
  anniversaries    Anniversary[]
  networkReminders NetworkReminder[]
}

model Anniversary {
  id        String   @id @default(uuid())
  contactId String
  type      String   // birthday, anniversary
  date      DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  contact Contact @relation(fields: [contactId], references: [id])
}

model Chore {
  id        String   @id @default(uuid())
  userId    String
  title     String
  frequency String
  nextDue   DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model ShoppingList {
  id        String   @id @default(uuid())
  userId    String
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user          User          @relation(fields: [userId], references: [id])
  shoppingItems ShoppingItem[]
}

model ShoppingItem {
  id             String   @id @default(uuid())
  shoppingListId String
  name           String
  quantity       Int
  purchased      Boolean  @default(false)
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  shoppingList ShoppingList @relation(fields: [shoppingListId], references: [id])
}

model InventoryItem {
  id         String   @id @default(uuid())
  userId     String
  name       String
  quantity   Int
  expiryDate DateTime?
  categoryId String?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt

  user               User               @relation(fields: [userId], references: [id])
  inventoryCategory  InventoryCategory? @relation(fields: [categoryId], references: [id])
}

model InventoryCategory {
  id        String   @id @default(uuid())
  name      String   // pantry, wardrobe
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  inventoryItems InventoryItem[]
}

model TravelPlan {
  id        String   @id @default(uuid())
  userId    String
  destination String
  startDate DateTime
  endDate   DateTime?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user             User             @relation(fields: [userId], references: [id])
  travelItineraries TravelItinerary[]
}

model TravelItinerary {
  id           String   @id @default(uuid())
  travelPlanId String
  time         DateTime
  activity     String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  travelPlan TravelPlan @relation(fields: [travelPlanId], references: [id])
}

model PetCareLog {
  id        String   @id @default(uuid())
  userId    String
  petId     String
  date      DateTime
  activity  String   // feed, walk
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
  pet  Pet  @relation(fields: [petId], references: [id])
}

model Pet {
  id        String   @id @default(uuid())
  userId    String
  name      String
  type      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user        User        @relation(fields: [userId], references: [id])
  petCareLogs PetCareLog[]
}

model SmartHomeIntegration {
  id        String   @id @default(uuid())
  userId    String
  deviceName String
  type      String
  config    Json?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id])
}

model CareerTracker {
  id          String   @id @default(uuid())
  userId      String
  role        String
  startDate   DateTime
  achievements Json?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user             User             @relation(fields: [userId], references: [id])
  careerMilestones CareerMilestone[]
}

model CareerMilestone {
  id               String   @id @default(uuid())
  careerTrackerId  String
  description      String
  date             DateTime
  createdAt        DateTime @default(now())
  updatedAt        DateTime @updatedAt

  careerTracker CareerTracker @relation(fields: [careerTrackerId], references: [id])
}

model NetworkReminder {
  id          String   @id @default(uuid())
  userId      String
  contactId   String
  message     String
  triggerTime DateTime
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user    User    @relation(fields: [userId], references: [id])
  contact Contact @relation(fields: [contactId], references: [id])
}

model CreativeNote {
  id        String   @id @default(uuid())
  userId    String
  content   String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user    User    @relation(fields: [userId], references: [id])
  sketches Sketch[]
}

model Sketch {
  id              String   @id @default(uuid())
  creativeNoteId  String
  imageUrl        String
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt

  creativeNote CreativeNote @relation(fields: [creativeNoteId], references: [id])
}